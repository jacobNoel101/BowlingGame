package bowlingSprites;

//import java.awt.*;
//import java.awt.geom.*;
import visual.dynamic.described.*;
//import visual.statik.described.*;

public class BowlingPin extends DescribedSprite
{
  // public BowlingPin(double x, double y)
  // {
  // AggregateContent c = new AggregateContent();
  // Content body = new Content();
  // body.setShape(new Rectangle2D.Double(507, 280, 12, 35));
  // body.setPaint(Color.WHITE);
  // Content stripe = new Content();
  // stripe.setShape(new Rectangle2D.Double(507, 290, 12, 4));
  // stripe.setPaint(Color.RED);
  // c.add(body);
  // c.add(stripe);
  // addKeyTime(0, new Point2D.Double(x, y), 0.0, 1.0, c);
  // addKeyTime(1, new Point2D.Double(x, y), 0.0, 1.0, c);
  // }

}
